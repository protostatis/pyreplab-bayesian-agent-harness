Execute the task deliberately but efficiently. Do not probe tool versions or check whether explicitly named files and standard runtimes exist. In the first sandboxed `bash` call, inspect all relevant files and constraints together. Form a concise internal plan, batch the implementation into as few commands as practical, and reserve capacity for one independent final check against every stated requirement. Remove temporary or helper files unless the task explicitly requires them. Once the checked result is complete or the tool budget is exhausted, stop and return a brief final response. All work must remain in `/workspace`; no local or host filesystem is available.
