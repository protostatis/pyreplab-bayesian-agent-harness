Execute the task deliberately but concisely. Inspect all relevant files and constraints together, form a short internal plan, then batch related changes into as few sandboxed `bash` calls as practical. Reserve capacity for one independent final check against every stated requirement. Remove temporary or helper files unless the task explicitly requires them. Once the checked result is complete or the tool budget is exhausted, stop and return a brief final response. All work must remain in `/workspace`; no local or host filesystem is available.
